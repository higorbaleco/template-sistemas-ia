const translations = {
  pt: {
    brand_eyebrow: "Estúdio local de limpeza",
    nav_overview: "Visão geral",
    nav_drive: "Upload ao Drive",
    nav_git: "Trilha Git",
    nav_delete: "Candidatos a apagar",
    nav_protected: "Protegidos",
    automation_stack: "Pilha de automação",
    rules: "Regras",
    rules_text: "Nada destrutivo acontece sozinho. O app classifica, prioriza e prepara a revisão humana.",
    status_ready: "Auditoria somente leitura pronta",
    hero_title: "Veja o Mac como uma central de controle, não como um navegador de arquivos.",
    hero_text: "Inventário, higiene de Git, duplicatas, candidatos ao Drive e zonas protegidas em uma única tela. O painel destaca o que está seguro, o que é arriscado e o que deve continuar local.",
    action_run_audit: "Rodar auditoria completa",
    action_refresh: "Atualizar painel",
    action_open_report: "Abrir relatório",
    metric_items_label: "Itens analisados",
    metric_size_label: "Volume total",
    metric_regenerable_label: "Regenerável",
    metric_reclaim_label: "Potencial de liberação",
    search_placeholder: "Buscar caminho, extensão ou nota",
    sort_largest: "Maiores primeiro",
    sort_smallest: "Menores primeiro",
    sort_path: "Caminho A-Z",
    sort_risk: "Risco primeiro",
    scan_inventory: "Escanear inventário",
    scan_apps: "Escanear apps",
    audit_git: "Auditar Git",
    find_duplicates: "Buscar duplicatas",
    action_queue: "Fila de ações",
    queue_title: "O que o sistema acha que devemos fazer agora",
    selected_item: "Item selecionado",
    inspector_title: "Inspector",
    inspector_empty: "Selecione uma linha para ver o caminho, tamanho e a ação sugerida.",
    top_heavy: "Itens pesados",
    top_heavy_title: "Arquivos e pastas maiores",
    risk_map: "Mapa de risco",
    risk_title: "Zonas protegidas e sensíveis",
    items_analyzed: "Itens analisados",
    disk_footprint: "Volume do disco",
    caches_assets: "Caches, builds e ativos regeneráveis",
    drive_candidates: "Candidatos ao Drive",
    cleanup_items: "Itens de limpeza",
    largest_first: "Maiores primeiro",
    smallest_first: "Menores primeiro",
    path_az: "Caminho A-Z",
    risk_first: "Risco primeiro",
    protected_zone: "Zona protegida",
    strong_drive_candidate: "Forte candidato a arquivo no Drive",
    manual_review: "Revisão manual recomendada",
    no_item_selected: "Nenhum item selecionado.",
    size: "Tamanho",
    type: "Tipo",
    top_level: "Pasta principal",
    confidence: "Confiança",
    extension: "Extensão",
    action: "Ação",
    selected_count: "itens",
    candidate_drive: "Upload ao Drive",
    candidate_git: "Trilha Git",
    candidate_delete: "Candidato a apagar",
    candidate_keep: "Manter local",
    candidate_document: "Documentar",
    protected_label: "PROTEGIDO",
    archive_label: "Arquivar",
    delete_label: "Apagar",
    git_review: "Revisar Git",
    keep_local: "Manter local",
    label_total_analyzed: "Itens analisados",
    label_disk_footprint: "Volume total",
    label_regenerable: "Regenerável",
    label_reclaim: "Potencial de liberação",
    summary_across_disk: "no disco",
    likely_archive: "itens provavelmente bons para arquivar",
    likely_cleanup: "itens confirmados ou prováveis para limpeza",
    select_language: "Idioma",
    open_app: "Abrir app",
    install_app: "Abrir .app",
    macro_title: "Pastas macro",
    macro_header: "Agrupe o workspace por raízes de projeto",
    macro_depth: "Profundidade",
    macro_selected: "Macro selecionada",
    macro_selected_empty: "Escolha uma pasta para inspecionar seus arquivos",
    macro_items: "itens",
    macro_size: "Tamanho",
    macro_heavy: "Mais pesados",
    macro_light: "Mais leves",
  },
  en: {
    brand_eyebrow: "Local cleanup studio",
    nav_overview: "Overview",
    nav_drive: "Drive Upload",
    nav_git: "Git Track",
    nav_delete: "Delete Candidates",
    nav_protected: "Protected",
    automation_stack: "Automation stack",
    rules: "Rules",
    rules_text: "Nothing destructive happens automatically. The app classifies, prioritizes, and prepares human review.",
    status_ready: "Read-only audit ready",
    hero_title: "See the Mac like a control room, not a file browser.",
    hero_text: "Inventory, Git hygiene, duplicates, Drive candidates, and protected zones in one layout. The panel highlights what is safe, what is risky, and what should stay local.",
    action_run_audit: "Run full audit",
    action_refresh: "Refresh panel",
    action_open_report: "Open report",
    metric_items_label: "Items analyzed",
    metric_size_label: "Disk footprint",
    metric_regenerable_label: "Regenerable",
    metric_reclaim_label: "Potential reclaim",
    search_placeholder: "Search path, extension or note",
    sort_largest: "Largest first",
    sort_smallest: "Smallest first",
    sort_path: "Path A-Z",
    sort_risk: "Risk first",
    scan_inventory: "Scan inventory",
    scan_apps: "Scan apps",
    audit_git: "Audit Git",
    find_duplicates: "Find duplicates",
    action_queue: "Action queue",
    queue_title: "What the system thinks we should do next",
    selected_item: "Selected item",
    inspector_title: "Inspector",
    inspector_empty: "Select a row to inspect its path, size, and suggested action.",
    top_heavy: "Top heavy items",
    top_heavy_title: "Largest files and folders",
    risk_map: "Risk map",
    risk_title: "Protected and sensitive zones",
    items_analyzed: "Items analyzed",
    disk_footprint: "Disk footprint",
    caches_assets: "Caches, builds, and rebuildable assets",
    drive_candidates: "Drive candidates",
    cleanup_items: "Cleanup items",
    largest_first: "Largest first",
    smallest_first: "Smallest first",
    path_az: "Path A-Z",
    risk_first: "Risk first",
    protected_zone: "Protected zone",
    strong_drive_candidate: "Strong candidate for Drive archive",
    manual_review: "Manual review recommended",
    no_item_selected: "No item selected.",
    size: "Size",
    type: "Type",
    top_level: "Top level",
    confidence: "Confidence",
    extension: "Extension",
    action: "Action",
    selected_count: "items",
    candidate_drive: "Drive Upload",
    candidate_git: "Git Track",
    candidate_delete: "Delete Candidate",
    candidate_keep: "Keep Local",
    candidate_document: "Document Only",
    protected_label: "PROTECTED",
    archive_label: "Archive",
    delete_label: "Delete",
    git_review: "Git review",
    keep_local: "Keep local",
    label_total_analyzed: "Items analyzed",
    label_disk_footprint: "Disk footprint",
    label_regenerable: "Regenerable",
    label_reclaim: "Potential reclaim",
    summary_across_disk: "across the disk",
    likely_archive: "items likely worth archiving",
    likely_cleanup: "confirmed or likely cleanup items",
    select_language: "Language",
    open_app: "Open app",
    install_app: "Open .app",
    macro_title: "Macro folders",
    macro_header: "Group the workspace by project roots",
    macro_depth: "Depth",
    macro_selected: "Selected macro",
    macro_selected_empty: "Pick a folder to inspect its files",
    macro_items: "items",
    macro_size: "Size",
    macro_heavy: "Heaviest",
    macro_light: "Lightest",
  },
};

const state = {
  data: null,
  query: "",
  filter: "all",
  sort: "size_desc",
  selectedPath: null,
  lang: "pt",
  macroDepth: 3,
  selectedMacro: null,
};

const $ = (id) => document.getElementById(id);

function t(key) {
  return translations[state.lang]?.[key] || translations.en[key] || translations.pt[key] || key;
}

function formatGB(value) {
  const number = Number(value || 0);
  return `${number.toFixed(number >= 10 ? 1 : 2)} GB`;
}

function cleanPath(path) {
  return String(path || "").replace(/^\/Users\/higorplens/, "~");
}

function normalizeMacroPath(path) {
  return String(path || "")
    .replace(/^\/Users\/higorplens\//, "")
    .replace(/^~\//, "")
    .replace(/^\/+/, "");
}

function macroKeyForPath(path, depth) {
  const segments = normalizeMacroPath(path).split("/").filter(Boolean);
  if (!segments.length) return "";
  return segments.slice(0, Math.min(depth, segments.length)).join("/");
}

function displayMacroName(key) {
  return key ? key.split("/").join(" / ") : "";
}

function buildMacroGroups(items, depth) {
  const groups = new Map();
  for (const item of items) {
    const key = macroKeyForPath(item.path, depth);
    if (!key) continue;
    const current = groups.get(key) || { key, count: 0, size_gb: 0, items: [] };
    current.count += 1;
    current.size_gb += Number(item.size_gb || 0);
    current.items.push(item);
    groups.set(key, current);
  }
  return [...groups.values()].sort((a, b) => b.size_gb - a.size_gb || b.count - a.count);
}

function selectedMacroItems(items) {
  if (!state.selectedMacro) return items;
  return items.filter((item) => macroKeyForPath(item.path, state.macroDepth) === state.selectedMacro);
}

function setLanguage(lang) {
  if (!translations[lang]) return;
  state.lang = lang;
  localStorage.setItem("mac-curator-lang", lang);
  document.documentElement.lang = lang === "pt" ? "pt-BR" : "en";
  document.title = lang === "pt" ? "Mac Curator" : "Mac Curator";
  document.querySelectorAll("[data-i18n]").forEach((node) => {
    const key = node.dataset.i18n;
    if (key && t(key)) node.textContent = t(key);
  });
  document.querySelectorAll("[data-i18n-placeholder]").forEach((node) => {
    const key = node.dataset.i18nPlaceholder;
    if (key && t(key)) node.placeholder = t(key);
  });
  const langSelect = $("languageSelect");
  if (langSelect && langSelect.value !== lang) langSelect.value = lang;
}

function deriveAction(item) {
  const explicit = item.acao_recomendada;
  if (explicit && explicit !== "nao-classificado") return explicit;
  if (item.regenerable_type) return "apagar-depois";
  if (item.duplicate_group) return "apagar-depois";
  if (item.git_status) return "git-atualizar";
  if (isProtected(item)) return "documentar";
  if (isDriveCandidate(item)) return "drive";
  if (isGitCandidate(item)) return "git-novo";
  return "manter";
}

function isDriveCandidate(item) {
  const path = String(item.path || "");
  const ext = String(item.ext || "").toLowerCase();
  const size = Number(item.size_gb || 0);
  return (
    (path.includes("/Documents/") || path.includes("/Downloads/") || path.includes("/Desktop/")) &&
    (size >= 0.05 || [".pdf", ".ppt", ".pptx", ".doc", ".docx", ".key", ".zip", ".rar", ".7z", ".mov", ".mp4", ".jpg", ".jpeg", ".png", ".svg"].includes(ext))
  );
}

function isGitCandidate(item) {
  const path = String(item.path || "").toLowerCase();
  const ext = String(item.ext || "").toLowerCase();
  return (
    path.includes("/projects/") ||
    path.includes("/code/") ||
    path.includes("/scripts/") ||
    path.includes("/src/") ||
    ext === ".md" ||
    ext === ".py" ||
    ext === ".ts" ||
    ext === ".tsx" ||
    ext === ".js" ||
    ext === ".jsx" ||
    ext === ".json" ||
    ext === ".css" ||
    ext === ".html"
  );
}

function isProtected(item) {
  const path = String(item.path || "");
  return (
    path.includes("/.ssh") ||
    path.includes("/.gnupg") ||
    path.includes("/Library/Keychains") ||
    path.includes("/Library/Mail") ||
    path.includes("/Library/Messages") ||
    path.includes("Photos Library.photoslibrary")
  );
}

function deriveRisk(item) {
  if (isProtected(item)) return "protected";
  if (item.regenerable_type || item.duplicate_group) return "delete";
  if (item.git_status) return "git";
  if (isDriveCandidate(item)) return "drive";
  return "keep";
}

function deriveConfidence(item) {
  if (item.acao_recomendada && item.acao_recomendada !== "nao-classificado") return 98;
  if (item.regenerable_type || item.duplicate_group) return 94;
  if (item.git_status) return 88;
  if (isDriveCandidate(item)) return 79;
  if (isProtected(item)) return 100;
  return 55;
}

function deriveLabel(item) {
  const action = deriveAction(item);
  const keyMap = {
    "apagar-depois": "candidate_delete",
    drive: "candidate_drive",
    "git-atualizar": "git_review",
    "git-novo": "candidate_git",
    documentar: "candidate_document",
    manter: "candidate_keep",
  };
  const key = keyMap[action];
  return key ? t(key) : action;
}

function deriveNote(item) {
  if (item.regenerable_type) return `${t("label_regenerable")}: ${item.regenerable_type}`;
  if (item.duplicate_group) return `Duplicate group ${item.duplicate_group}`;
  if (item.git_status) return `Git ${item.git_status}${item.git_ahead_behind ? ` · ${item.git_ahead_behind}` : ""}`;
  if (isDriveCandidate(item)) return t("strong_drive_candidate");
  if (isProtected(item)) return t("protected_zone");
  return item.motivo || t("manual_review");
}

function normalizedItems() {
  const items = (state.data?.items || []).map((item) => ({
    ...item,
    derived_action: deriveAction(item),
    derived_risk: deriveRisk(item),
    derived_label: deriveLabel(item),
    derived_confidence: deriveConfidence(item),
    derived_note: deriveNote(item),
  }));

  return selectedMacroItems(items).filter((item) => {
    const search = state.query.trim().toLowerCase();
    if (state.filter !== "all") {
      if (state.filter === "drive" && item.derived_risk !== "drive") return false;
      if (state.filter === "git" && !item.derived_risk.startsWith("git")) return false;
      if (state.filter === "delete" && item.derived_action !== "apagar-depois") return false;
      if (state.filter === "protected" && item.derived_risk !== "protected") return false;
    }
    if (!search) return true;
    return [item.path, item.ext, item.top_level, item.derived_action, item.derived_label, item.derived_note]
      .filter(Boolean)
      .some((value) => String(value).toLowerCase().includes(search));
  });
}

function sortItems(items) {
  const copy = [...items];
  switch (state.sort) {
    case "size_asc":
      return copy.sort((a, b) => (a.size_gb || 0) - (b.size_gb || 0));
    case "path_asc":
      return copy.sort((a, b) => String(a.path || "").localeCompare(String(b.path || "")));
    case "risk_desc":
      return copy.sort((a, b) => {
        const order = { protected: 4, delete: 3, drive: 2, git: 1, keep: 0 };
        return (order[b.derived_risk] || 0) - (order[a.derived_risk] || 0) || (b.size_gb || 0) - (a.size_gb || 0);
      });
    case "size_desc":
    default:
      return copy.sort((a, b) => (b.size_gb || 0) - (a.size_gb || 0));
  }
}

function renderSummary() {
  const summary = state.data?.summary || {};
  const cards = [
    {
      label: t("label_total_analyzed"),
      value: (summary.total_items || 0).toLocaleString("en-US"),
      detail: `${formatGB(summary.total_size_gb || 0)} ${t("summary_across_disk")}`,
    },
    {
      label: t("label_regenerable"),
      value: formatGB(summary.total_regenerable_gb || 0),
      detail: t("caches_assets"),
    },
    {
      label: t("drive_candidates"),
      value: formatGB(summary.estimated_drive_gb || 0),
      detail: `${state.data?.derived?.drive_candidates || 0} ${t("likely_archive")}`,
    },
    {
      label: t("label_reclaim"),
      value: formatGB(summary.estimated_delete_gb || 0),
      detail: `${state.data?.derived?.delete_candidates || 0} ${t("likely_cleanup")}`,
    },
  ];

  $("summaryGrid").innerHTML = cards
    .map(
      (card) => `
        <article class="stat-card">
          <div class="label">${card.label}</div>
          <div class="value">${card.value}</div>
          <div class="detail">${card.detail}</div>
        </article>
      `,
    )
    .join("");

  $("metricItems").textContent = (summary.total_items || 0).toLocaleString("en-US");
  $("metricSize").textContent = formatGB(summary.total_size_gb || 0);
  $("metricRegenerable").textContent = formatGB(summary.total_regenerable_gb || 0);
  $("metricDelete").textContent = formatGB(summary.estimated_delete_gb || 0);
}

function renderMacroFolders() {
  const items = (state.data?.items || []).map((item) => ({
    ...item,
    derived_action: deriveAction(item),
    derived_risk: deriveRisk(item),
    derived_label: deriveLabel(item),
    derived_confidence: deriveConfidence(item),
    derived_note: deriveNote(item),
  }));
  const groups = buildMacroGroups(items, state.macroDepth);

  if (!state.selectedMacro && groups.length) {
    state.selectedMacro = groups[0].key;
  }

  $("macroCount").textContent = `${groups.length}`;
  $("macroList").innerHTML = groups
    .map(
      (group) => `
      <div class="macro-item ${group.key === state.selectedMacro ? "is-selected" : ""}" data-macro="${encodeURIComponent(group.key)}">
        <div class="macro-name">${displayMacroName(group.key)}</div>
        <div class="macro-badge">${group.count} ${t("macro_items")}</div>
        <div class="macro-badge">${formatGB(group.size_gb)}</div>
        <div class="macro-badge">${group.items.filter((entry) => entry.derived_action === "apagar-depois").length} delete</div>
      </div>
    `,
    )
    .join("");

  document.querySelectorAll("[data-macro]").forEach((node) => {
    node.addEventListener("click", () => {
      state.selectedMacro = decodeURIComponent(node.dataset.macro);
      renderMacroFolders();
      renderMacroSelected();
      renderQueue();
      renderTopHeavy();
      renderRiskMap();
      renderInspector();
    });
  });
}

function renderMacroSelected() {
  const items = (state.data?.items || []).map((item) => ({
    ...item,
    derived_action: deriveAction(item),
    derived_risk: deriveRisk(item),
    derived_label: deriveLabel(item),
    derived_confidence: deriveConfidence(item),
    derived_note: deriveNote(item),
  }));
  const groups = buildMacroGroups(items, state.macroDepth);
  const selected = groups.find((group) => group.key === state.selectedMacro);
  const title = $("macroSelectedTitle");
  const panel = $("macroSelectedSummary");

  if (!selected) {
    title.textContent = t("macro_selected_empty");
    panel.innerHTML = `<div class="empty-state">${t("macro_selected_empty")}</div>`;
    return;
  }

  title.textContent = displayMacroName(selected.key);
  const heavy = [...selected.items].sort((a, b) => (b.size_gb || 0) - (a.size_gb || 0)).slice(0, 6);
  const light = [...selected.items].sort((a, b) => (a.size_gb || 0) - (b.size_gb || 0)).slice(0, 6);
  const deletes = selected.items.filter((entry) => entry.derived_action === "apagar-depois").length;

  panel.innerHTML = `
    <div class="detail-card">
      <div class="detail-grid">
        <div><span>${t("macro_items")}</span><strong>${selected.count}</strong></div>
        <div><span>${t("macro_size")}</span><strong>${formatGB(selected.size_gb)}</strong></div>
        <div><span>${t("delete_label")}</span><strong>${deletes}</strong></div>
        <div><span>${t("selected_item")}</span><strong>${displayMacroName(selected.key)}</strong></div>
      </div>
    </div>
    <div class="macro-breakdown">
      <div class="macro-subgrid">
        <div class="macro-list-card">
          <h4>${t("macro_heavy")}</h4>
          <div class="macro-file-list">
            ${heavy
              .map(
                (item) => `
              <div class="macro-file-row" data-focus="${encodeURIComponent(item.path)}">
                <span>${cleanPath(item.path)}</span>
                <strong>${formatGB(item.size_gb || 0)}</strong>
              </div>`,
              )
              .join("")}
          </div>
        </div>
        <div class="macro-list-card">
          <h4>${t("macro_light")}</h4>
          <div class="macro-file-list">
            ${light
              .map(
                (item) => `
              <div class="macro-file-row" data-focus="${encodeURIComponent(item.path)}">
                <span>${cleanPath(item.path)}</span>
                <strong>${formatGB(item.size_gb || 0)}</strong>
              </div>`,
              )
              .join("")}
          </div>
        </div>
      </div>
    </div>
  `;

  panel.querySelectorAll("[data-focus]").forEach((node) => {
    node.addEventListener("click", () => {
      state.selectedPath = decodeURIComponent(node.dataset.focus);
      renderQueue();
      renderInspector();
    });
  });
}

function renderAutomation() {
  const list = $("automationList");
  list.innerHTML = (state.data?.automation || [])
    .map(
      (module) => `
      <div class="module">
        <div class="module-top">
          <div class="module-title">${module.label}</div>
          <div class="module-status ${module.status}">${module.status}</div>
        </div>
        <div class="module-path">${cleanPath(module.path)}</div>
      </div>
    `,
    )
    .join("");
}

function renderQueue() {
  const items = sortItems(normalizedItems()).slice(0, 80);
  $("resultCount").textContent = `${items.length} ${t("selected_count")}`;
  $("queueList").innerHTML = items
    .map(
      (item) => `
      <div class="queue-item ${state.selectedPath === item.path ? "is-selected" : ""}" data-path="${encodeURIComponent(item.path)}">
        <div>
          <div class="item-path">${cleanPath(item.path)}</div>
          <div class="item-meta">${item.top_level || "root"} · ${item.ext || "dir"} · ${formatGB(item.size_gb || 0)}</div>
        </div>
        <div class="badge ${item.derived_risk}">${item.derived_label}</div>
        <div class="item-meta">Confidence ${item.derived_confidence}%</div>
        <div class="item-note">${item.derived_note}</div>
      </div>
    `,
    )
    .join("");

  document.querySelectorAll(".queue-item").forEach((node) => {
    node.addEventListener("click", () => {
      state.selectedPath = decodeURIComponent(node.dataset.path);
      renderQueue();
      renderInspector();
    });
  });
}

function renderTopHeavy() {
  const items = sortItems(normalizedItems()).slice(0, 12);
  $("topHeavy").innerHTML = items
    .map(
      (item) => `
      <div class="table-row" data-path="${encodeURIComponent(item.path)}">
        <div class="row-top">
          <div class="row-title">${cleanPath(item.path)}</div>
          <div class="panel-chip">${formatGB(item.size_gb || 0)}</div>
        </div>
        <div class="row-sub">${item.derived_label} · ${item.derived_note}</div>
      </div>
    `,
    )
    .join("");

  document.querySelectorAll("#topHeavy .table-row").forEach((node) => {
    node.addEventListener("click", () => {
      state.selectedPath = decodeURIComponent(node.dataset.path);
      renderQueue();
      renderInspector();
    });
  });
}

function renderRiskMap() {
  const summary = [
    { label: t("protected_label"), value: state.data?.summary?.estimated_protected_gb || 0, chip: "DO_NOT_TOUCH" },
    { label: t("git_review"), value: state.data?.summary?.estimated_git_gb || 0, chip: t("git_review") },
    { label: t("archive_label"), value: state.data?.summary?.estimated_drive_gb || 0, chip: t("archive_label") },
    { label: t("delete_label"), value: state.data?.summary?.estimated_delete_gb || 0, chip: t("delete_label") },
  ];

  $("riskMap").innerHTML = summary
    .map(
      (entry) => `
      <div class="risk-row">
        <div class="row-top">
          <div class="row-title">${entry.label}</div>
          <div class="risk-chip">${entry.chip}</div>
        </div>
        <div class="row-sub">${formatGB(entry.value || 0)}</div>
      </div>
    `,
    )
    .join("");
}

function renderInspector() {
  const item = normalizedItems().find((entry) => entry.path === state.selectedPath) || normalizedItems()[0];
  if (!item) {
    $("inspector").innerHTML = `<div class="empty-state">${t("no_item_selected")}</div>`;
    return;
  }

  state.selectedPath = item.path;
  $("inspector").innerHTML = `
    <div class="detail-card">
      <h4>${cleanPath(item.path)}</h4>
      <div class="badge ${item.derived_risk}">${item.derived_label}</div>
      <p class="item-note" style="margin-top:10px;">${item.derived_note}</p>
      <div class="detail-grid" style="margin-top:14px;">
        <div><span>${t("size")}</span><strong>${formatGB(item.size_gb || 0)}</strong></div>
        <div><span>${t("type")}</span><strong>${item.type || "file"}</strong></div>
        <div><span>${t("top_level")}</span><strong>${item.top_level || "-"}</strong></div>
        <div><span>${t("confidence")}</span><strong>${item.derived_confidence}%</strong></div>
        <div><span>${t("extension")}</span><strong>${item.ext || "-"}</strong></div>
        <div><span>${t("action")}</span><strong>${item.derived_action}</strong></div>
      </div>
    </div>
  `;
}

async function fetchState() {
  const res = await fetch("/api/state", { cache: "no-store" });
  state.data = await res.json();
}

async function runJob(action) {
  const body = new URLSearchParams({ action });
  const res = await fetch("/api/run", {
    method: "POST",
    headers: { "Content-Type": "application/x-www-form-urlencoded" },
    body,
  });
  return res.json();
}

async function refreshPanel() {
  await fetchState();
  setLanguage(state.lang);
  renderSummary();
  renderMacroFolders();
  renderMacroSelected();
  renderAutomation();
  renderQueue();
  renderTopHeavy();
  renderRiskMap();
  renderInspector();
}

function bindControls() {
  $("searchInput").addEventListener("input", (event) => {
    state.query = event.target.value;
    renderQueue();
    renderTopHeavy();
    renderInspector();
  });

  $("sortSelect").addEventListener("change", (event) => {
    state.sort = event.target.value;
    renderQueue();
    renderTopHeavy();
  });

  $("macroDepthSelect").addEventListener("change", (event) => {
    state.macroDepth = Number(event.target.value || 3);
    state.selectedMacro = null;
    renderMacroFolders();
    renderMacroSelected();
    renderQueue();
    renderTopHeavy();
    renderRiskMap();
    renderInspector();
  });

  $("languageSelect").addEventListener("change", (event) => {
    setLanguage(event.target.value);
    renderSummary();
    renderAutomation();
    renderQueue();
    renderTopHeavy();
    renderRiskMap();
    renderInspector();
  });

  document.querySelectorAll("[data-filter]").forEach((node) => {
    node.addEventListener("click", () => {
      document.querySelectorAll("[data-filter]").forEach((btn) => btn.classList.remove("is-active"));
      node.classList.add("is-active");
      state.filter = node.dataset.filter;
      renderQueue();
      renderTopHeavy();
      renderInspector();
    });
  });

  document.querySelectorAll("[data-open]").forEach((node) => {
    node.addEventListener("click", async () => {
      const target = node.dataset.open;
      if (target === "report") {
        await fetch("/api/open/report");
      }
      if (target === "app") {
        await fetch("/api/open/app");
      }
    });
  });

  document.querySelectorAll("[data-run]").forEach((node) => {
    node.addEventListener("click", async () => {
      const action = node.dataset.run;
      await runJob(action);
      setTimeout(refreshPanel, 1200);
    });
  });
}

function initLanguageFromQuery() {
  const params = new URLSearchParams(window.location.search);
  const fromQuery = params.get("lang");
  const fromStorage = localStorage.getItem("mac-curator-lang");
  const fromBrowser = (navigator.language || "pt").toLowerCase().startsWith("pt") ? "pt" : "en";
  state.lang = [fromQuery, fromStorage, fromBrowser].find((value) => translations[value]) || "pt";
}

async function bootstrap() {
  initLanguageFromQuery();
  await refreshPanel();
  bindControls();
  setLanguage(state.lang);
  setInterval(async () => {
    try {
      await fetchState();
      renderSummary();
      renderAutomation();
    } catch (error) {
      console.warn(error);
    }
  }, 15000);
}

bootstrap();
